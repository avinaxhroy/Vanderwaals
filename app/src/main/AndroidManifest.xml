<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SET_WALLPAPER"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Strip permissions auto-implied by the LiteRT/TFLite GPU library
         (litert-gpu-api targets an old SDK and requests WRITE_EXTERNAL_STORAGE,
         which forces READ_EXTERNAL_STORAGE + READ_PHONE_STATE into the merged
         manifest). These are not used by the app and would trigger Google Play
         Photo/Video Permissions and Phone State policy rejections. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE" tools:node="remove"/>

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:name="me.avinas.vanderwaals.VanderwaalsApplication"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.Vanderwaals.SplashScreen" tools:targetApi="tiramisu">
        <activity
            android:name=".feature.wallpaper.presentation.MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Vanderwaals.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- DeviceUnlockReceiver - DISABLED: Replaced by WallpaperMonitorService
             which runs as a foreground service to reliably receive ACTION_USER_PRESENT
             on Android 8.0+ (API 26+).
        -->
        <receiver android:name="me.avinas.vanderwaals.receiver.DeviceUnlockReceiver"
            android:enabled="false"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>

        <!-- WallpaperMonitorService - Foreground Service for "Every Unlock" -->
        <!-- CRITICAL: Must be foreground service to receive ACTION_USER_PRESENT -->
        <service
            android:name=".service.WallpaperMonitorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        
        <!-- WallpaperChangeService - Short-lived foreground service for scheduled changes -->
        <!-- Started by WallpaperAlarmReceiver to handle wallpaper changes when app is killed -->
        <service
            android:name=".service.WallpaperChangeService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        
        <!-- BootCompletedReceiver - Reschedules workers after device reboot -->
        <!-- CRITICAL: Ensures auto-change feature works after phone restart -->
        <!-- Requires RECEIVE_BOOT_COMPLETED permission -->
        <receiver
            android:name="me.avinas.vanderwaals.receiver.BootCompletedReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
        
        <!-- WallpaperAlarmReceiver - Handles daily wallpaper change alarms -->
        <!-- CRITICAL: Triggers wallpaper change at exact scheduled time -->
        <!-- Uses setAndAllowWhileIdle() so it fires even in Doze mode -->
        <receiver
            android:name="me.avinas.vanderwaals.worker.WallpaperAlarmReceiver"
            android:enabled="true"
            android:exported="false">
        </receiver>

        <!-- WorkManager initialization provider -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- SystemForegroundService for WorkManager -->
        <!-- CRITICAL: Specifies foreground service type for workers using setForegroundAsync() -->
        <!-- Used by BatchDownloadWorker for downloading wallpapers with dataSync foreground service type -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>

