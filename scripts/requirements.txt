# Python dependencies for Vanderwaals wallpaper curation pipeline
# Install with: pip install -r requirements.txt

# Deep Learning - TensorFlow for MobileNetV3 embeddings
tensorflow-cpu  # Use tensorflow-cpu for github actions

# Image Processing
Pillow                  # Image loading, color extraction, resizing
imagehash              # Perceptual hashing for deduplication

# Machine Learning
numpy                   # Array operations, embeddings
scikit-learn             # K-means clustering for color extraction

# CLI & Progress
tqdm                  # Progress bars for long-running tasks

# Networking - GitHub API for incremental updates
requests              # HTTP client for GitHub API
cloudscraper          # Bypass Cloudflare protection on bing.npanuhin.me

# Note: Git is required for repository cloning (pre-installed on GitHub Actions)
